# Production Database Configuration
spring.datasource.url=jdbc:oracle:thin:@oracle-db:1521/FREEPDB1
spring.datasource.username=sneaker_user
spring.datasource.password=nguyen123
spring.datasource.driver-class-name=oracle.jdbc.OracleDriver

# JPA Configuration (giống development)
spring.jpa.hibernate.ddl-auto=update

# Hibernate naming strategy removed - using default behavior

# App Properties
projectjavasneaker.app.jwtSecret=${JWT_SECRET:is216SecretKey}
projectjavasneaker.app.jwtExpirationMs=86400000

# Email Configuration (nếu cần)
spring.mail.host=smtp.gmail.com
spring.mail.port=587
spring.mail.username=${EMAIL_USERNAME:jguyene@gmail.com}
spring.mail.password=${EMAIL_PASSWORD:pyfchuavrxlaqvnr}
spring.mail.properties.mail.smtp.auth=true
spring.mail.properties.mail.smtp.starttls.enable=true
spring.mail.properties.mail.transport.protocol=smtp

# Server Configuration
server.port=8080
server.servlet.context-path=/

# Logging
logging.level.com.projectjavasneaker=INFO
logging.level.org.springframework.security=DEBUG
logging.level.org.hibernate.SQL=DEBUG
logging.level.org.hibernate.type.descriptor.sql.BasicBinder=TRACE

# CORS Configuration (nếu cần)
# spring.web.cors.allowed-origins=http://localhost:3000,http://localhost:80
# spring.web.cors.allowed-methods=GET,POST,PUT,DELETE,OPTIONS
# spring.web.cors.allowed-headers=*
